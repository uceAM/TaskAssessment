namespace TaskAssessment.Data.Constants;

public static class Enums
{
    public enum StatusEnum
    {
        pending = 1,
        inProgress,
        complete,
    };
    public enum RoleEnum
    {
        admin,
        manager,
        employee
    }
}
